I now have everything I need. Here is the full implementation plan:

---

# Phase 18 â€” Templates: Implementation Plan

## 1. Files to CREATE

### `lib/WES2_widgets/WES2_template_picker.dart` (new)

A bottom-sheet function `showWes2TemplatePicker(BuildContext, String uid) â†’ Future<Template?>`.

- Displays a `DraggableScrollableSheet` (maxChildSize 0.85) via `showModalBottomSheet`.
- Fetches `users/{uid}/templates` collection from Firestore (single read, no listener needed).
- Groups results: active-block templates first, other blocks next, no-block templates last.
- Each item is a simple `InkWell` row â€” title with `maxLines: 1, overflow: TextOverflow.ellipsis`, optional subtitle (day/block label). No `ListTile` inside `PopupMenuItem`.
- Empty state: single centered "No templates saved" text.
- All Row children are `Expanded` or `Flexible`. No unconstrained text. Safe at 360px width.
- Returns the tapped `Template` object, or null on dismiss.

**Import:** `package:cloud_firestore/cloud_firestore.dart`, `template_model.dart`.

---

## 2. Files to MODIFY

### `lib/WES2_template_service.dart`

**Replace** the Phase-1 stub with a full `FirestoreWes2TemplateService` class implementing the existing abstract `Wes2TemplateService` interface. No changes to the abstract methods.

**`loadTemplate` implementation:**
- Fetches `users/{uid}/templates/{templateId}` from Firestore.
- If doc does not exist â†’ returns `[]`.
- Parses `exercises` list using the same pattern as `Template.fromFirestore`:
  - Expects each entry to be a `Map<String, dynamic>` with `name` (String), `circuitIndex` (int, default 0), and optionally `exerciseId`, `orderIndex`, `setCount`.
  - **Silent skip rule:** if an entry has no `exerciseId` key (legacy templates created without WES2), skip it â€” exerciseId is the mandatory row identity key in WES2.
  - `setCount` defaults to 3 if absent.
  - `orderIndex` defaults to list index if absent.
  - Sets `source = Wes2RowSource.templateLoaded`.
  - Creates `Wes2SetState(setIndex: i)` blank entries up to `setCount` â€” no actual values, no hint values. These are structure-only rows.
- Returns `List<Wes2ExerciseRow>`.

**`saveWorkoutAsTemplate` implementation:**
- Auto-generates a doc ID in `users/{uid}/templates/`.
- Stores: `name` (templateName), `blockId`, `createdAt: FieldValue.serverTimestamp()`.
- `exercises` array: one map per row with `{ exerciseId, name, circuitIndex, orderIndex, setCount }` only. **Never** stores weight, reps, rir, velocity, executionNote, or any actual value.
- Does NOT store `blockAssignment` (requires block metadata lookup, out of scope for Phase 18).

---

### `lib/WES2_controller.dart`

**Add one new field:**
```dart
bool _loadedFromTemplate = false;
```

**Add two new getters:**
```dart
bool get loadedFromTemplate => _loadedFromTemplate;

bool get showSaveToTemplatesLabel {
  if (_loadedFromTemplate) return false;
  if (_rows.isEmpty) return false;
  return _rows.every((r) => r.isMarkedDone);
}
```

**Modify `changeDate()`:** add `_loadedFromTemplate = false;` alongside the existing resets.

**Add `applyTemplateRows(List<Wes2ExerciseRow> templateRows) â†’ bool` method:**

- Calls `_pushUndo()` first (snapshot before replace â€” this drives the undo snackbar).
- Collects `completedRows` = rows with `source == Wes2RowSource.completedServer` (preserved verbatim).
- Builds `completedIds` set from those rows.
- Deduplicates `templateRows`: for each, if `exerciseId` is in `completedIds` or already seen, silently skip.
- Sets `_rows = [...completedRows, ...deduped]`.
- Sets `_loadedFromTemplate = true`.
- Updates `_loadState` accordingly (empty or loaded).
- Calls `notifyListeners()`.
- **Returns** `hadExistingRows` (whether `_rows` was non-empty before replace) â€” the screen uses this to decide whether to show the undo snackbar.

No changes to any existing method signatures or behaviors.

---

### `lib/WES2_repository.dart`

**Add to abstract interface:**
```dart
Future<void> replaceWesPlannedWithTemplateRows({
  required String uid,
  required DateTime date,
  required List<Wes2ExerciseRow> templateRows,
});
```

**Add concrete implementation** in `FirestoreWes2Repository`:

- Same Firestore transaction pattern as existing methods, writing to `users/{uid}/workouts/{dateDocId}`.
- Reads current `exercises[]` â€” kept entirely untouched.
- Builds new `wesPlannedExercises[]` from `templateRows` via `_buildWesPlannedRowMap()` (already exists).
- Server-side duplicate guard: skip any templateRow whose `exerciseId` appears in `exercises[]`.
- Writes with `SetOptions(merge: true)`, same shape as all other write methods.
- No new Firestore paths. No BB3 writes (see BLOCKED section below).

---

### `lib/WES2_screen.dart`

**Wire `_templateService`:** Add `final Wes2TemplateService _templateService = FirestoreWes2TemplateService();` to `_Wes2ScreenState` if not already present. (Current grep shows no existing instance.)

**Replace** the `_showTemplatesPlaceholder()` call at line ~520 with `_loadTemplate()`.

**Remove** the `_showTemplatesPlaceholder()` method (~lines 937-943).

**Add `_loadTemplate()` async method:**
1. Calls `showWes2TemplatePicker(context, _controller.actingUid)`.
2. If null returned â†’ return early (user dismissed).
3. Calls `_templateService.loadTemplate(uid: ..., templateId: template.id)` inside a try/catch; on error shows a snackbar and returns.
4. Calls `_controller.applyTemplateRows(templateRows)` â†’ captures `hadRows`.
5. Calls `_silentlySave(...)` (existing pattern) wrapping `_repository.replaceWesPlannedWithTemplateRows(...)`.
6. If `hadRows == true`: shows undo snackbar using the existing undo snackbar pattern already in the screen (other structural operations already use this pattern). Undo action calls `_controller.undo()` and re-saves the reverted `wesPlannedExercises[]` to Firestore.

**Add `_showSaveAsTemplateDialog()` async method:**
1. Shows `AlertDialog` with a `TextField` (label "Template name", default text "Day").
2. On confirm: trims name; if empty â†’ no-op.
3. Calls `_silentlySave(...)` wrapping `_templateService.saveWorkoutAsTemplate(uid: ..., blockId: _controller.activeBlockId ?? '', templateName: name, rows: _controller.rows.toList())`.
4. Shows a brief confirmation snackbar: `Saved as "$name"`.

**In `build`:** Pass `showSaveToTemplates: _controller.showSaveToTemplatesLabel` and `onSaveToTemplates: _showSaveAsTemplateDialog` to `Wes2BottomActionsRow`.

---

### `lib/WES2_widgets/WES2_day_actions_row.dart`

**Modify `Wes2BottomActionsRow`:**

Add two optional parameters:
```dart
final bool showSaveToTemplates;        // default false
final VoidCallback? onSaveToTemplates;
```

Add inside the existing `Column`, after the actions `Row` and before `SizedBox(height: 55)`:
```dart
if (showSaveToTemplates && onSaveToTemplates != null)
  Padding(
    padding: const EdgeInsets.only(top: 4, left: 8),
    child: TextButton(
      onPressed: onSaveToTemplates,
      style: TextButton.styleFrom(
        foregroundColor: Colors.white38,
        padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),
        minimumSize: Size.zero,
        tapTargetSize: MaterialTapTargetSize.shrinkWrap,
      ),
      child: const Text(
        'Save workout to templates',
        style: TextStyle(fontSize: 11),
        overflow: TextOverflow.ellipsis,
        maxLines: 1,
      ),
    ),
  ),
```

All existing callers pass only the original parameters â€” the new ones default to `false`/`null`, so no existing call sites break.

---

## 3. Reference Files Read (NOT edited)

- `lib/templates.dart` â€” confirmed Firestore path (`users/{uid}/templates/{docId}`), exercise map schema (`name`, `circuitIndex`, optional `exerciseId`/`orderIndex`/`setCount`), and template grouping logic.
- `lib/template_model.dart` â€” `Template` class: `id, name, day, blockId, blockAssignment, exercises: List<Map>`, `fromFirestore` backward-compat parsing.

---

## 4. Blocked Files â€” Will NOT edit

`lib/workout_entry_screen.dart`, `lib/main.dart`, `lib/bb3_*.dart`, `lib/templates.dart`, `lib/template_model.dart`, `lib/periodization_model_utils.dart`, `lib/progression_engine.dart`, `pubspec.yaml`, `assets/**`, `*.g.dart`, `*.freezed.dart`, `.claude/settings.local.json`.

---

## 5. Phase-Specific Guidance â€” Point by Point

| Requirement | Status | Where handled |
|---|---|---|
| Template loading replaces day rows (not appends) | âœ“ | `applyTemplateRows` replaces non-completed rows |
| Undo snackbar if existing rows replaced; no snackbar if empty | âœ“ | `applyTemplateRows` returns `hadExistingRows`; screen branches on it |
| Blank template rows persist in `wesPlannedExercises[]` | âœ“ | `replaceWesPlannedWithTemplateRows` writes to `wesPlannedExercises[]`; template rows have no actual values and are never written to `exercises[]` |
| "Save to templates" stores structure only (no weight/reps/etc.) | âœ“ | `saveWorkoutAsTemplate` stores `exerciseId, name, circuitIndex, orderIndex, setCount` only |
| Template picker overflow-safe at 360px | âœ“ | `WES2_template_picker.dart`: all text in `Expanded`/`Flexible`, `maxLines: 1`, `overflow: ellipsis`; no `ListTile` inside `PopupMenuItem` |
| Deduplicate exerciseIds on load | âœ“ | `applyTemplateRows` and `replaceWesPlannedWithTemplateRows` both deduplicate; silently skip |
| Snapshot undo state BEFORE replacing | âœ“ | `applyTemplateRows` calls `_pushUndo()` as first action |
| Do not show "save to templates" if day loaded from template | âœ“ | `_loadedFromTemplate` flag; `showSaveToTemplatesLabel` returns false when set |
| Show "save to templates" only when all rows isMarkedDone | âœ“ | `showSaveToTemplatesLabel` uses `.every((r) => r.isMarkedDone)` |
| Language must not imply workout values are unsaved | âœ“ | Dialog title "Save as Template"; label text matches spec verbatim |
| Preserve all existing WES2 behaviors | âœ“ | Template load is a net-new action; no existing method signatures changed |
| Reset `_loadedFromTemplate` on date change | âœ“ | `changeDate()` resets the flag |

---

## 6. BB3 CLEARING â€” BLOCKED

**The spec says:** "If replacing BB3 planned exercises, clear entire BB3 planned day."

**Assessment: BLOCKED.**

After reading `WES2_repository.dart` in full: every write method targets only `users/{uid}/workouts/{dateDocId}`. There are **no existing BB3 (planned\_blocks) write paths anywhere in the WES2 codebase**. The structural operations added in Phases 13-14 (deleteExercise, replaceExercise, moveExerciseToCircuit) all write only to the WES2 workout doc.

The BB3 planned day path (likely something under `planned_blocks/{uid}/blocks/{blockId}/...`) is defined in `bb3_*.dart` files. Additionally, `Wes2SessionController` tracks `_activeBlockId` but does **not** track `weekIndex` or `dayIndex`, which are required to address the specific BB3 day sub-document. `WES2_plan_service.dart` reads BB3 to hydrate WES2, but the reverse write direction (WES2 clearing a BB3 planned day) has never been established.

**What IS safe and WILL be implemented:** The WES2 workout document's `wesPlannedExercises[]` will be replaced with template rows (the WES2-owned array). BB3-sourced rows are evicted from the in-memory display. The WES2 workout document will be correct.

**What requires human confirmation before implementation:**
1. The exact Firestore path for the BB3 planned day (the `planned_blocks` sub-document that holds the day's planned exercises).
2. The field name(s) to clear on that document.
3. Whether `weekIndex` and `dayIndex` context is already plumbed through `WES2_plan_service` to the screen, or needs to be threaded through.

Once those are confirmed, a `clearBb3PlannedDay(uid, blockId, weekIndex, dayIndex)` method can be added to `WES2_repository.dart` (which is in the allowed write scope) and called from `_loadTemplate()` after template rows are applied.
